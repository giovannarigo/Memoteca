export interface PensamentoInterface {
  id?: number;
  conteudo: string;
  autoria: string;
  modelo: string;
}
//Obrigatóriamente todos as classes, objetos e/ou arrays que forem herdados por essa interface, terão que ter todos esses atributos, especificamente nessa tipagem.
